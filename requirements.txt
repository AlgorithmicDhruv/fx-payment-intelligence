# FX Payment Intelligence Engine — Dependencies
# Python 3.10+ | Install: pip install -r requirements.txt

# ── Core data ──────────────────────────────────────────────────
numpy>=1.24.0           # compatible with both 1.x and 2.x
pandas>=2.0.0
pyarrow>=12.0.0         # parquet support
yfinance>=0.2.37
fredapi>=0.5.2
requests>=2.31.0

# ── Statistics & features ──────────────────────────────────────
scipy>=1.11.0
statsmodels>=0.14.0
arch>=6.0.0             # GARCH models

# ── ML & forecasting ───────────────────────────────────────────
scikit-learn>=1.3.0
torch>=2.0.0            # CPU is fine — model is 107K params
shap>=0.43.0

# ── RL (optional) ──────────────────────────────────────────────
stable-baselines3>=2.0.0
gymnasium>=0.29.0

# ── RAG ────────────────────────────────────────────────────────
# Embeddings: HuggingFace Inference API (BAAI/bge-small-en-v1.5)
# No chromadb — was incompatible with NumPy 2.x
# No sentence-transformers — caused OOM on 4GB machines
# Vector store: plain JSON, pure-Python cosine similarity
huggingface_hub>=0.23.0   # official HF SDK for inference API

# ── Dashboard ──────────────────────────────────────────────────
streamlit>=1.35.0
plotly>=5.18.0
fpdf2>=2.7.0              # PDF export in dashboard

# ── Utilities ──────────────────────────────────────────────────
python-dotenv>=1.0.0
loguru>=0.7.0
joblib>=1.3.0